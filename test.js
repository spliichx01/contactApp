window.$onclick = function(target, type, cb){

	target.addEventListener(type, cb, false);
}

var CORE = (function(){


	var modeules = {};

	function addModule(module_id, mod){

		modules[module_id] = mod;
	}

	function registerEvents




})();